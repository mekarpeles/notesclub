# Book Notes Club

## Backend

### Create db:
rails db:create
rails db:migrate

### Start server:
`rails s`

## Start frontend
`cd front`
`yarn start`

Open `http://hec.localhost.hec:3001` in a browser.
