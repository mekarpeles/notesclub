require 'rails_helper'

RSpec.describe SearchController do
  describe "#count" do
    xit "should count non-root topics regardless of descendants" do
    end

    xit "should only count root topics if their eldests are not empty" do
    end

    xit "should return 10 if the count is higher" do
    end
  end
end
